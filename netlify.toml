# Netlify Configuration for CashPulse Frontend

[build]
  # Build command
  command = "cd frontend && npm install && npm run build"
  
  # Directory with the build output
  publish = "frontend/dist"
  
  # Base directory for build
  base = "/"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

# Redirect rules for SPA (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Environment variables (set these in Netlify dashboard)
# VITE_API_URL = your backend API URL
# VITE_SOCKET_URL = your backend socket URL
# VITE_PLATFORM_NAME = CashPulse
# VITE_SUPPORT_EMAIL = blessedsuccess538@gmail.com
# VITE_WHATSAPP_URL = https://wa.me/2348085430370
# VITE_TELEGRAM_CHANNEL = https://t.me/makingonline217

# Functions (if you want to use Netlify Functions for API)
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
